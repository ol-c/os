{
  "name": "ol-c-terminal-client",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test src/*.test.mjs",
    "watch": "node build.mjs --watch"
  },
  "dependencies": {
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/addon-unicode11": "^0.9.0",
    "@xterm/xterm": "^5.5.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.3"
  }
}
